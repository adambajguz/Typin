codecov:
  max_report_age: 24h

coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        threshold: 0.2
        if_not_found: success
    patch:
      default:
        if_no_uploads: success
    changes:
      default:
        if_not_found: success