namespace Typin.Console.IO
{
    /// <summary>
    /// Abstraction for interacting with the standard output stream (stdout) and error stream (stderr).
    /// </summary>
    public interface IStandardOutputAndError : IStandardOutput, IStandardError
    {

    }
}
